[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "niri"
version = "0.1.0"
description = "Niri IPC client library"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/niri"]
